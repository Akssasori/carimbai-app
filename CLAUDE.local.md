# carimbai-app — overrides pessoais (NÃO commitado)

Este arquivo é seu — fica fora do git (ver `.gitignore`). Use para anotações e
overrides que valem só na sua máquina.

<!--
Exemplos:

## API
- Apontar o front para staging/prod em vez do localhost:
  ajuste VITE_API_BASE_URL no .env (atenção: o .env atual NÃO está no
  .gitignore — evite colocar segredos reais nele).

## Client-ids
- Meu VITE_GOOGLE_CLIENT_ID de testes: ...

## Lembretes
- Subir o backend ../carimbai antes de testar fluxos que chamam a API.
-->
